{
  "fixedSectionOk": true,
  "nodesRecovered": 0,
  "edgesRestored": 0,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "Merge report had no 'Fixed' or 'Could not fix' issues; only the earlier UTF-8 BOM issue (already resolved by rewriting batch-existing.json without BOM before this run). Report's stale 'Output: 455 edges' line predates the imports recovery; the actual file contains 482 edges = 455 batch edges + 27 recovered imports edges, fully consistent.",
    "Structural sanity verified on assembled-graph.json: 315 nodes (125 file, 173 function, 10 config, 6 document, 1 pipeline) and 482 edges. No duplicate node IDs, no missing id/type/name/summary/tags/complexity fields, no empty summaries, all complexity values in {simple, moderate, complex}, all node ID prefixes match their type, no dangling edge refs, no self-loops, no duplicate edges, no weight anomalies.",
    "27 imports edges are flagged recoveredFromImportMap, all weight 0.7 and direction forward, all backed by importMap pairs, none dangling.",
    "Cross-batch import gap check against importMap (143 entries, 108 import pairs): all 106 pairs whose source AND target file nodes both exist already have an imports edge; 0 edges needed. No speculative edges added.",
    "18 importMap sources have no file: node in the graph; all are legitimately excluded (root/config/docs files: README.md, AGENTS.md, package.json, tsconfig*, vercel.json, .github workflows, .slim docs, build artifacts) or deleted per the change set (tests/piercer.test.ts, whose 2 outgoing import pairs cannot be represented). No action needed.",
    "15 imports edges in the graph are batch-derived (not in importMap pairs, e.g. PropellingFunTracker.svelte -> src/lib/store.ts); they are backed by batch analysis and kept as-is.",
    "No remaining concerns."
  ]
}
